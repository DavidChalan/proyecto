apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 3000         # Puerto interno
      targetPort: 3000   # Puerto del contenedor
      nodePort: 30000    # Puerto del nodo (expuesto al exterior)
